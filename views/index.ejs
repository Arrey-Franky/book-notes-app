<!DOCTYPE html>
<html>
<head>
  <title>My Book Notes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        padding: 40px;
        background: linear-gradient(135deg, #6a11cb, #2575fc);
        color: #fff;
        margin: 0;
    }

    .grid {
        display: flex;
        flex-wrap: wrap;
        gap: 30px;
        justify-content: center;
    }

    .card {
        width: 250px;
        padding: 15px;
        border-radius: 16px;
        background: linear-gradient(145deg, #ffffff, #f0f0f0);
        color: #333;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        transition: transform 0.3s ease, box-shadow 0.3s ease, background 0.3s ease;
        overflow: hidden;
    }

    .card:hover {
        transform: translateY(-10px);
        box-shadow: 0 16px 32px rgba(0, 0, 0, 0.25);
        background: linear-gradient(145deg, #dfe9f3, #ffffff);
    }

    .card img {
        width: 100%;
        border-radius: 12px;
        margin-bottom: 12px;
    }

    .card h3 {
        margin: 0 0 8px 0;
        font-size: 20px;
        color: #222;
    }

    .card p {
        font-size: 14px;
        color: #555;
        line-height: 1.5;
    }

    .card .btn {
        display: block;
        width: 100%;
        margin-top: 10px;
        padding: 8px 0;
        border-radius: 8px;
        text-align: center;
        font-size: 14px;
        text-decoration: none;
        color: #fff;
        cursor: pointer;
        border: none;
    }

    .btn-primary { background-color: #2575fc; }
    .btn-primary:hover { background-color: #6a11cb; }

    .btn-secondary { background-color: #6c757d; }
    .btn-secondary:hover { background-color: #5a6268; }

    .btn-success { background-color: #28a745; }
    .btn-success:hover { background-color: #218838; }

    .btn-danger { background-color: #dc3545; }
    .btn-danger:hover { background-color: #c82333; }
  </style>
</head>

<body>
  <h1>📚 My Book Notes</h1>

  <a href="/new"><button class="btn btn-primary">➕ Add Book</button></a>
  <br><br>

  <div class="grid">
    <% books.forEach(book => { %>
      <div class="card">

         

        <h3><%= book.title %></h3>
        <p><strong>Author:</strong> <%= book.author %></p>
        <p><strong>Rating:</strong> <%= book.display_rating || book.rating %> ⭐</p>
        <p><strong>Published:</strong> <%= book.published_date %></p>
        <p><strong>Note:</strong> <%= book.notes %></p>

        <% if (book.link_url) { %>
          <a href="<%= book.cover_url %>" target="_blank" class="btn btn-success">📖 View Book</a>
        <% } %>

        <a href="/edit/<%= book.id %>" class="btn btn-secondary">✏ Edit</a>

        <form action="/delete/<%= book.id %>" method="POST">
          <button class="btn btn-danger">🗑 Delete</button>
        </form>

      </div>
    <% }) %>
  </div>

</body>
</html>
